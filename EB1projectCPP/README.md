Source code extracted from EB1projectCPP.zip (the src/ and include/ folders only -- build artifacts, binaries, and IDE project files from the zip are not duplicated here). Some files are identical to the versions in HungarianAlgorithm_cCode_LAP/, others (e.g. main.cpp) are modified for this project -- see EB1projectCPP.zip for the full original build folder, including the compiled executables.
